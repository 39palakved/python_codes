# touple is ordered anf immutable(unchangeble),allow duplicates
# other three list, dictionary and set are also used to store collection of elements with diffrent properties
thistuple = ('a','b','c')
print(thistuple)
for i in thistuple :
    print(i)
